#import "../template.typ": todo

= AI 应用支持探索

前两章围绕兼容性内核的核心子系统与关键机制展开，使 StarryOS具备了承载Linux应用的基础能力与性能优化路径。然而，AIoT 场景对操作系统的需求不止于通用兼容性，还涉及专用硬件加速器集成与 AI 推理框架运行支持。本章从这一方向出发，探讨面向 NPU 等边缘侧加速器的驱动设计思路，以及在定制化内核上支撑典型 AI 应用运行的可行性分析。需要说明的是，本章工作目前处于探索与规划阶段，部分内容尚未完全落地，更多是对后续演进方向的梳理与初步尝试。

== 目标平台与硬件环境

#todo[
  简述目标开发板（配备 NPU 的边缘计算设备），
  说明需要完成的移植工作（StarryOS 跨架构支持、驱动适配）。
]

== NPU 驱动设计

#todo[
  简述 NPU 驱动开发的思路和挑战：
  驱动模型、与内核内存管理的交互、DMA 支持等。
]

== AI 应用运行支持

#todo[
  简述目标应用（YOLO 目标检测、dora-rs 数据流框架）的运行需求，
  以及 StarryOS 需要提供的支持（推理框架兼容性、实时调度等）。
]

== 当前进展与后续计划

#todo[如果工作尚未完成，可在此说明当前进展、阶段性结果及后续计划。]

#import "../template.typ": todo

= 系统背景与总体架构

第 1 章已经介绍了操作系统内核架构演进、组件化操作系统理念以及 AIoT 场景对操作系统的核心需求，并指出构建兼具兼容性、组件化与高性能的内核所面临的技术挑战。本章在此基础上，进一步阐明 StarryOS 的系统定位与设计目标，并从总体分层、执行模型和核心资源对象三个层面概述系统结构，为后续各核心子系统实现与关键机制优化提供统一的框架。

== 设计目标

StarryOS 的首要目标，是在 ArceOS 组件化基座之上构建一个具备较强 Linux 应用兼容能力的宏内核系统，使其能够承载更丰富的用户态程序、开发工具链和典型服务程序运行。与单纯面向专用化任务的内核不同，兼容性目标要求系统在进程管理、文件系统、信号、网络、终端和事件机制等方面逐步对齐 Linux/POSIX 语义，从而使现有软件生态能够以较低成本迁移到系统之上。

在兼容性之外，StarryOS 还强调组件化复用与性能优化并重。一方面，系统需要延续 ArceOS 的组件化思想，将与具体内核形态弱相关或可抽象的能力拆分为独立模块，以降低工程耦合并支持后续开源复用；另一方面，兼容性内核在系统调用路径、用户态访存、I/O 处理和缓存管理等位置存在明显性能压力，因此需要围绕关键路径进行专门优化。进一步地，面向 AIoT 场景的后续演化还要求系统具备良好的跨架构移植能力以及接入专用硬件与 AI 应用运行时的潜力。因此，StarryOS 的总体目标可以概括为：在组件化基础上实现完整兼容性，并在此基础上持续追求工程可维护性与系统性能。

== ArceOS 基础与 StarryOS 定位

=== ArceOS 的灵活内核理念

ArceOS 的设计出发点，是避免将操作系统定制工作绑定在某一种固定内核架构之上。传统定制内核通常围绕单一形态展开，当应用需求从专用化运行转向更强的隔离性或更丰富的兼容性时，已有工作往往难以直接迁移，只能重新开发或退回通用内核。为降低这一迁移成本，ArceOS 提出了灵活内核的设计原则，即将不同内核形态共享的基础服务抽象为可复用组件，而将内核最终表现出的形态视为初始化完成后执行逻辑的结果。这样一来，同一套底层能力便可以被组织为 Unikernel、宏内核或 Hypervisor 等不同系统。

在实现路径上，ArceOS 采用组件化方法构建硬件抽象、运行时支撑和上层功能模块，并强调组件之间通过明确接口进行协作。该设计的价值不在于预先规定唯一正确的系统结构，而在于为不同内核形态保留足够的组合空间，使得上层系统能够在共享底座能力的前提下针对目标场景进行定制。对于本文所研究的 StarryOS 而言，ArceOS 提供的不仅是一个可复用的底层基座，更是一种将兼容性内核实现建立在组件化原则之上的方法论基础。

=== ArceOS 的组件层次结构

从实现组织上看，ArceOS 及其上层扩展通常可以分为若干层次。最底层是与硬件平台和执行环境直接相关的基础支撑，负责硬件抽象、系统初始化以及运行时支持；在其之上是围绕内存管理、任务调度、文件系统、网络、设备驱动等组织的核心功能模块；再向上则是一些能够在不同内核形态中复用的通用组件；最上层才是具体系统形态对这些能力的组合与使用。这样的层次划分有助于将"底层公共能力"和"上层系统策略"分离开来。

对于本文所讨论的系统而言，也可以沿着这一思路来理解其构建路径：底层复用组件化基座提供的硬件抽象与运行时能力，中间层承载内存、任务、文件系统、网络等基本操作系统功能，而上层则在此基础上实现面向 Linux 兼容性的宏内核功能。图 X 给出了总体层次结构，说明了各层之间的依赖关系。

=== StarryOS 的系统定位

StarryOS 可以看作 ArceOS 在 Linux 兼容性宏内核方向上的一条重要扩展路径。与 ArceOS 原本更强调灵活内核与组件复用的实验性基座定位不同，StarryOS 更直接地面向用户态程序运行环境，要求系统能够提供完整的进程抽象、地址空间管理、文件与设备抽象、网络接口以及信号与事件处理机制，从而承载更接近 Linux 使用习惯的软件生态。因此，StarryOS 并不是对 ArceOS 基础模块的简单堆叠，而是在复用底座能力的同时，将大量 POSIX/Linux 兼容语义逐步落实到具体实现中。

对于本文而言，更重要的并不是工程目录如何命名，而是系统在结构上如何划分职责边界。整体上看，StarryOS 在下层继承了组件化基座提供的通用能力，在上层围绕兼容性需求组织进程、内存、文件、网络和信号等核心功能，并通过若干相对独立的模块承载特定语义。这种定位既保留了组件化设计带来的可复用性，也使系统能够朝着完整兼容性内核的方向持续演化。

== 系统总体架构

=== 总体分层与模块关系

从总体上看，StarryOS 的结构可以概括为"组件化基座 + 宏内核主体 + 若干相对独立的功能模块"三部分。底层部分负责提供硬件抽象、运行时支撑以及调度、内存、文件系统、网络等基础能力；中间的宏内核主体负责组织用户程序加载、系统调用分发以及多个子系统之间的协作；上层则围绕兼容性需求实现进程管理、信号处理、虚拟内存访问等具体功能。这样的组织方式使系统既能复用底层能力，又能在主体部分集中实现 Linux 兼容性相关逻辑。

从功能关系上看，系统调用层位于用户态与内核态交互路径的中心位置，负责将陷入内核的请求分派到任务管理、内存管理、文件系统、网络与信号等子系统；文件系统和网络子系统又分别通过统一的对象抽象与设备、缓存和事件机制发生关联；任务管理与调度机制则为整个系统提供执行上下文；信号、poll/epoll、futex 等同步与事件相关机制横向贯穿多个子系统，构成阻塞、唤醒和中断语义的统一基础。后续章节将沿着这些模块边界，分别展开对核心子系统和关键机制的介绍。

=== 组件边界与仓库组织

StarryOS 的工程组织延续了 ArceOS 的组件化拆分思路，完整系统包含 30 余个相关代码仓库，分别面向底层基础能力、宏内核特有功能以及通用工具抽象。这种组织方式通过接口隔离来控制依赖方向，使不同层次的功能能够在尽可能小的影响范围内迭代。

在仓库组织上，系统遵循"与具体系统形态弱相关的能力尽量独立、与宏内核实现强绑定的逻辑集中维护"的原则。本文后续在描述系统结构时，重点放在功能边界划分与依赖关系控制上。

== 内核执行模型

=== 用户态与内核态的基本切换流程

在 StarryOS 中，用户态程序与内核态之间的切换主要通过系统调用、异常和中断三种途径触发。当用户程序执行陷入指令（如 x86_64 的 `syscall` 或 RISC-V 的 `ecall`）时，处理器硬件自动完成特权级切换、保存用户上下文并跳转到预定义的内核入口点。内核入口代码负责进一步保存完整的用户态寄存器状态，构建内核栈帧，随后进入统一的系统调用分发路径。

系统调用分发由 `handle_syscall` 函数统一处理。该函数首先从用户上下文中提取系统调用号，通过 `Sysno` 类型进行合法性校验，随后根据调用号将请求分派到对应子系统处理函数。例如，文件相关系统调用（`openat`、`read`、`write` 等）被路由到文件系统模块，进程管理相关调用（`clone`、`execve`、`wait4` 等）被路由到任务管理模块。每个系统调用处理函数完成具体逻辑后返回结果，分发函数将返回值写入用户上下文的返回值字段，最后通过异常返回指令恢复用户态执行。

对于异常和中断，处理流程与系统调用类似但入口点不同。页异常触发地址空间的缺页处理逻辑，由 `AddrSpace::handle_page_fault` 根据映射后端类型完成懒分配或写时复制操作；外设中断则通过中断控制器分发到对应设备驱动处理。整个陷入-处理-返回路径构成了内核执行的基础骨架，后续介绍的进程管理、内存管理和事件机制都围绕这一路径展开。

=== 进程、线程与任务对象关系

StarryOS 的执行实体抽象建立在 ArceOS 的任务（Task）概念之上，同时引入进程（Process）和线程（Thread）两层结构以对齐 Linux 语义。在 ArceOS 中，任务是最基本的调度单位，由 `axtask` 模块管理，包含任务标识符、状态、调度优先级、内核栈和任务扩展（TaskExt）等核心属性。StarryOS 并不直接修改 ArceOS 的任务定义，而是通过 `extern_trait` 机制将自定义的 `Thread` 结构挂接到任务扩展字段中，从而在不侵入底层调度器的前提下承载 Linux 兼容性所需的额外状态。

进程（Process）由独立的 `starry_process` crate 定义，是进程关系和线程组的容器。每个进程包含进程标识符（PID）、线程组、父进程引用和子进程集合、所属进程组和会话等核心属性。进程组（`ProcessGroup`）和会话（`Session`）实现了作业控制和终端管理所需的层级关系。为支持 POSIX 语义中进程组和会话的隐式创建与回收，系统设计了反向存储结构：进程持有进程组的强引用，而进程组持有进程的弱引用表。该弱引用表由专门开发的 `weak-map` crate 实现，在进程退出时自动失效，无需显式清理。

线程（Thread）由内核模块定义，持有对进程数据的引用，同时维护线程级信号管理器、定时器状态等线程特有属性。每个线程通过 `Box<Thread>` 形式存储在任务扩展中，实现了从 ArceOS 任务到 StarryOS 线程的无缝桥接。进程数据是进程级资源的集中管理点，包括地址空间、文件描述符表、命令行参数、信号处理器表等。这种"进程共享、线程独立"的资源划分方式与 Linux 的进程/线程模型高度一致。

== 核心资源管理对象

在介绍完执行模型之后，本节从资源对象角度概括 StarryOS 的组织方式。兼容性内核的核心职责是将用户程序看到的抽象对象（进程、文件、地址空间等）映射到内核内部的具体数据结构，并保证这些对象在生命周期管理、并发访问和跨子系统协作中的一致性。后续各子系统实现章节将围绕这些对象展开，此处先给出整体视角。

=== 地址空间与内存对象

地址空间（`AddrSpace`）是 StarryOS 虚拟内存管理的核心对象，封装了虚拟地址范围、内存区域集合（`MemorySet`）和页表（`PageTable`）三部分。每个进程拥有独立的地址空间实例，由 `ProcessData` 持有并通过互斥锁保护。地址空间支持映射、取消映射、权限修改和缺页处理等基本操作，其内部通过统一的 Backend 抽象支持不同类型的映射后端，包括线性映射、按需分配、写时复制和文件映射等。

内存区域（`MemoryArea`）描述一段连续虚拟地址的属性，包括起始地址、大小、权限标志和后端类型。后端（`Backend`）决定了该区域的物理页分配策略：`LinearBackend` 用于固定物理地址映射，`CowBackend` 支持写时复制的按需分配，`FileBackend` 将文件内容映射到内存并支持页缓存，`SharedBackend` 用于跨进程共享内存。这种分层设计使 `mmap`、`brk` 等系统调用能够统一处理匿名映射、文件映射和共享映射，同时为后续缓存优化提供清晰的承载点。

=== 文件、设备与 I/O 对象

StarryOS 采用类 Unix 的文件抽象模型，通过 `FileLike` trait 统一定义文件、管道、套接字、事件 fd 和信号 fd 等可读写对象的行为。`FileLike` 提供读取、写入、状态查询、ioctl 控制和非阻塞标志设置等接口，同时继承 `Pollable` trait 以支持事件等待机制。每个打开的文件由 `FileDescriptor` 结构描述，包含指向具体 `FileLike` 实现的引用以及 close-on-exec 标志。文件描述符表通过 `Scope` 机制管理，支持按进程隔离以及 `clone` 时共享或复制。

普通文件由 `File` 结构表示，内部持有对文件系统后端（`FileBackend`）的引用，后者可能是页缓存或直接设备访问。设备和伪文件系统通过 VFS 层纳入统一访问框架，`Device` trait 提供设备特有的 mmap 支持和 ioctl 处理。套接字由 `Socket` 结构表示，封装了 TCP、UDP 和 Unix Domain Socket 等网络通信能力。管道、事件 fd 和信号 fd 分别由 `Pipe`、`EventFd` 和 `Signalfd` 实现，均遵循 `FileLike` 接口。这种统一抽象使 `read`、`write`、`poll`、`epoll_ctl` 等系统调用能够跨不同对象类型工作，简化了事件驱动和 I/O 多路复用的实现。

=== 通信与事件对象

进程间通信和事件驱动机制在 StarryOS 中由 socket、poller、waker 和 signal 等对象共同支撑。套接字（`Socket`）封装网络协议栈提供的通信能力，同时作为 `FileLike` 对象可被纳入文件描述符表并通过 epoll 监控。事件等待机制由 `PollSet`、`Pollable` 和 `Waker` 三层构成，为 poll/select/epoll、futex 等待、子进程退出等待等场景提供统一基础。

信号机制由独立的 `starry_signal` crate 实现，采用进程级和线程级两层管理结构。`ProcessSignalManager` 维护进程级待处理信号队列和信号处理器表，`ThreadSignalManager` 维护线程级待处理队列、信号掩码和信号栈。信号投递时根据目标选择进程或线程队列，并在系统调用返回路径检查和处理。

== 本章小结

本章基于第 1 章提出的问题背景，进一步阐明了 StarryOS 的设计目标与系统定位。系统以 Linux 应用兼容性为核心目标，在 ArceOS 组件化基座之上构建宏内核形态，同时兼顾组件化复用与关键路径性能优化。本章还从总体分层、执行模型和核心资源对象三个维度概述了系统结构，为后续各核心子系统实现与关键机制优化提供了整体框架。

#import "../template.typ": todo

= 核心子系统设计与实现

本章在第 2 章总体架构的基础上，进一步介绍 StarryOS 兼容性内核中若干核心子系统的组织方式与实现基础。与第 4 章聚焦关键机制和性能优化不同，本章更关注系统作为一个完整 Linux 兼容性内核所必须具备的“基本盘”，即系统如何承载用户程序的执行、如何管理地址空间与文件对象、如何处理网络与终端输入输出，以及如何通过信号与同步机制组织进程间交互。通过这些内容，可以更清晰地理解后续各项优化工作是建立在怎样的系统基础之上的。

== 系统调用框架

系统调用框架是 StarryOS 连接用户态程序与内核子系统的核心入口。当用户程序通过陷入指令进入内核后，系统首先根据处理器提供的上下文信息识别系统调用号及参数，随后进入统一的系统调用分发路径，再由内核将请求转交给文件、内存、任务、时间、信号等具体子系统处理，最后将返回值写回用户上下文并恢复执行。从设计目标上看，该框架既要尽可能贴近 Linux 的系统调用语义，又要避免将过多具体逻辑堆叠在单一路径中，从而保持模块边界清晰。

从当前代码组织来看，系统调用相关逻辑主要集中在 `starry-kernel` 的 `syscall` 模块下，并按照资源、信号、时间等主题进一步划分子模块。这种组织方式体现出一种较为稳妥的折中：对外保持统一的系统调用入口，对内则按照功能域将实现分散到相对独立的模块中，减少不同子系统之间的直接耦合。对于兼容性内核而言，这种结构也更有利于逐步补齐系统调用支持范围，并在后续演化中持续维护语义一致性。
从实现组织来看，系统调用相关逻辑采用了“统一入口、按功能域分解实现”的方式：对外保持一致的系统调用进入路径，对内则按照资源、信号、时间等主题划分子模块，减少不同子系统之间的直接耦合。对于兼容性内核而言，这种结构也更有利于逐步补齐系统调用支持范围，并在后续演化中持续维护语义一致性。

== 任务与进程管理

#todo[
  简述 fork/exec/wait/exit/clone 等核心进程管理系统调用的实现要点，
  重点说明与 ArceOS 任务模型的对接方式，以及任务、线程、进程对象的组织。
]

== 内存管理

#todo[简述 mmap/munmap/brk 等的实现，涵盖匿名映射、文件映射、懒分配、CoW 等机制。]

== 文件系统

StarryOS 的文件系统设计遵循类 Unix 系统常见的 VFS 分层思路，即通过统一抽象将具体物理文件系统、伪文件系统以及设备文件纳入同一访问框架。在这一框架下，文件描述符负责向进程暴露统一的访问句柄，`file` 对象承载一次打开后的状态，`inode` 与目录项等对象则承担对底层命名空间和存储实体的组织。这种设计的意义在于，将进程看到的文件操作接口与具体文件系统实现解耦，从而能够在相同系统调用语义下支持普通磁盘文件、管道、套接字以及 `/proc`、`/dev` 等特殊文件。

结合较新的设计文档与当前代码结构可以看到，StarryOS 侧既需要处理普通文件系统，也需要实现 `procfs`、`tmpfs`、`devfs` 等伪文件系统支持，因此 VFS 层不仅是功能复用的基础，也是后续缓存优化的重要承载点。页缓存、块缓存和目录项缓存等机制虽然将在后文重点讨论，但它们能够发挥作用的前提，正是系统已经建立起统一的文件对象与路径解析框架，使不同来源的数据访问都可以被纳入一致的管理路径中。

== 网络与终端子系统

网络与终端子系统共同构成了 StarryOS 面向真实用户空间应用的重要外部交互能力。根据设计文档，系统网络栈建立在 `smoltcp` 之上，并在其外封装 POSIX 风格的 socket 抽象，以支持 TCP、UDP 和 Unix Domain Socket 等常见通信方式；同时，系统还逐步补齐了多设备接入和与事件机制协同工作的能力。当前代码中，网络相关逻辑主要分布在文件抽象和底层网络模块的结合处，这也反映出“套接字即文件对象”的 Unix 风格设计思路。

终端相关能力则主要面向交互式应用和开发工具链支持。为了运行 shell、编辑器以及更复杂的图形与文本应用，系统需要提供 tty/pty、行规程以及作业控制等基础机制，使应用能够在接近 Linux 的终端语义下运行。这些能力本身未必体现出特别强的创新性，但它们对于兼容性内核的完整性至关重要，也是后续展示 Alpine Linux、开发工具链和图形环境支持时不可缺少的系统基础。

== 信号与同步机制

信号与同步机制贯穿于 StarryOS 的多个子系统之中，是实现类 Linux 进程交互语义的重要支撑。信号机制负责在异步事件发生时向目标进程或线程传递通知，并要求系统在合适的边界上检查待处理信号、构造用户态处理上下文并恢复执行；同步机制则需要为进程阻塞、等待和唤醒提供统一基础，包括 futex、poll/select/epoll 等常见设施。两者虽然分别服务于不同语义目标，但在兼容性内核内部往往共享一套事件等待与唤醒路径。

从系统组织角度看，信号机制并不是一个孤立模块，而是与系统调用返回路径、任务状态切换以及 I/O 事件处理紧密结合。特别是在阻塞系统调用场景下，信号能否正确打断等待过程、系统是否能够按照预期返回错误码或执行重启语义，直接决定了用户态应用的兼容性表现。也正因如此，本文后续在讨论异步机制时，还需要进一步说明信号与事件唤醒路径之间的配合关系。

== 本章小结

#todo[
  总结本章对兼容性内核基本盘的实现，
  说明这些基础子系统为后续关键机制与性能优化提供了承载平台。
]

#import "../template.typ": todo

= 核心子系统设计与实现

本章在第 2 章总体架构的基础上，进一步介绍 StarryOS 兼容性内核中若干核心子系统的组织方式与实现基础。与第 4 章聚焦关键机制和性能优化不同，本章更关注系统作为一个完整 Linux 兼容性内核所必须具备的"基本盘"，即系统如何承载用户程序的执行、如何管理地址空间与文件对象、如何处理网络与终端输入输出，以及如何通过信号与同步机制组织进程间交互。通过这些内容，可以更清晰地理解后续各项优化工作是建立在怎样的系统基础之上的。

== 系统调用框架

系统调用框架是 StarryOS 连接用户态程序与内核子系统的核心入口。当用户程序通过陷入指令进入内核后，系统首先根据处理器提供的上下文信息识别系统调用号及参数，随后进入统一的系统调用分发路径，再由内核将请求转交给文件、内存、任务、时间、信号等具体子系统处理，最后将返回值写回用户上下文并恢复执行。从设计目标上看，该框架既要尽可能贴近 Linux 的系统调用语义，又要避免将过多具体逻辑堆叠在单一路径中，从而保持模块边界清晰。

系统调用相关逻辑主要集中在 `starry-kernel` 的 `syscall` 模块下，采用"统一入口、按功能域分解实现"的组织方式：对外保持一致的系统调用进入路径，对内则按照资源、信号、时间等主题划分子模块，减少不同子系统之间的直接耦合。这种结构有利于逐步补齐系统调用支持范围，并在后续演化中持续维护语义一致性。

== 任务与进程管理

任务与进程管理子系统负责用户程序的创建、执行、终止和状态查询，是实现 Linux 进程模型的核心。该子系统围绕 `clone`/`fork`、`execve`、`wait4` 和 `exit` 四类系统调用展开，同时在实现上与 ArceOS 的任务调度框架紧密协作。

进程创建通过 `clone` 系统调用实现，该调用接收一组标志位控制父子进程间的资源共享行为。`CLONE_VM` 标志使子进程共享父进程的地址空间，对应线程创建场景；`CLONE_FILES` 和 `CLONE_FS` 标志分别控制文件描述符表和文件系统信息（当前目录、根目录）的共享；`CLONE_SIGHAND` 控制信号处理器表的共享；`CLONE_THREAD` 将新线程加入父进程的线程组。`fork` 系统调用在 x86_64 架构上实现为 `clone(SIGCHLD)` 的简化形式。实现上，`CloneArgs::do_clone` 方法负责参数校验、新任务创建、地址空间克隆（通过 `AddrSpace::try_clone` 实现 CoW）、资源复制或共享、新线程挂接到进程，最后将任务加入调度队列。

程序加载与替换由 `execve` 系统调用处理。该调用接收可执行文件路径、命令行参数和环境变量，负责清空当前地址空间（保留 CoW 映射的原始页）、加载 ELF 文件、设置新的入口点和栈顶地址、更新进程元数据（路径、命令行）、重置信号处理器为默认值、关闭带 close-on-exec 标志的文件描述符，最后修改用户上下文以跳转到新程序入口。execve 保持进程标识符和线程不变，仅替换地址空间内容，这与 Linux 的语义一致。

进程对象由独立的 `starry_process` crate 定义，内部包含线程组（`ThreadGroup`）结构。线程组记录属于同一进程的所有线程 ID 集合、进程退出码以及组退出标志（`group_exited`）。当调用 `exit_group` 系统调用时，组退出标志被设置，表示整个进程正在终止。进程还维护父进程的弱引用和子进程的强引用集合，支持 `wait4` 系统调用查询子进程状态。

进程组和会话管理是作业控制的基础。`setsid` 系统调用创建新会话并使调用进程成为会话首进程和进程组首进程，`setpgid` 将进程加入指定进程组。会话-进程组-进程构成一个树状层级结构，但与常规树状存储不同，POSIX 语义中进程组和会话是隐式创建和回收的：进程调用 `setsid` 时隐式创建会话和进程组，进程退出时隐式从所属容器中移除。为支持这种隐式生命周期管理，系统设计了反向存储结构：进程持有进程组的强引用（`Arc<ProcessGroup>`），进程组持有进程的弱引用表（`WeakMap<Pid, Weak<Process>>`）。当进程退出时，其弱引用自动失效，无需显式调用清理操作。

为支持这一设计，系统专门开发了 `weak-map` crate。该 crate 基于 `BTreeMap` 实现弱引用映射，提供 `WeakMap` 和 `StrongMap` 两种类型。`WeakMap` 存储弱引用，在迭代访问时自动跳过已失效的条目；同时实现了惰性清理机制，在操作计数达到阈值时自动移除过期条目。这一设计避免了显式生命周期管理的复杂性，使进程组和会话的管理代码更加简洁。

线程对象由内核模块中的 `Thread` 结构定义，持有对进程数据（`ProcessData`）的共享引用。线程维护线程级信号管理器（`ThreadSignalManager`）、定时器状态（`TimeManager`）、退出标志（`exit`）以及 robust futex 列表等线程特有属性。每个线程通过 `Box<Thread>` 形式存储在 ArceOS 任务扩展（`AxTaskExt`）中，通过 `extern_trait` 机制实现 `on_enter` 和 `on_leave` 回调，在任务切换时自动激活和切换进程资源作用域。

进程数据是进程级资源的集中管理点，包括地址空间（`AddrSpace`）、文件描述符表（通过 `Scope` 管理）、命令行参数、信号处理器表、futex 表以及进程退出事件等。所有属于同一进程的线程共享同一 `ProcessData` 实例，而不同进程的线程则拥有独立的 `ProcessData`。这种"进程共享、线程独立"的资源划分方式与 Linux 的进程/线程模型高度一致，也是实现 `clone` 系统调用中各种共享标志语义的基础。

进程终止通过 `exit` 和 `exit_group` 系统调用实现。前者终止当前线程，后者终止整个进程的所有线程。实现上，线程退出时从线程组中移除自身并记录退出码，当最后一个线程退出时触发进程退出。进程退出时标记为僵尸状态，其子进程被 init 进程接管，父进程通过 `wait4` 或 `waitpid` 系统调用查询退出状态并释放僵尸进程资源。wait 实现采用异步等待模式，通过 `block_on` 和 `interruptible` 包装使等待过程可被信号打断，符合 POSIX 的可中断系统调用语义。

== 内存管理

内存管理子系统负责用户进程地址空间的创建、映射、扩展和回收，是兼容性内核中与硬件交互最频繁的部分。StarryOS 的内存管理建立在 `AddrSpace` 核心对象之上，通过多种后端（Backend）类型支持不同映射场景，同时实现了按需分配、写时复制等关键优化机制。

`brk` 系统调用用于扩展或收缩进程堆区域。实现上，系统维护一个 `heap_top` 变量记录当前堆顶地址，新分配请求在预定义的堆范围内按页对齐扩展映射。`brk` 不立即分配物理页，而是仅建立虚拟到物理的映射关系，实际物理分配在首次访问时通过缺页处理完成。这种懒分配策略减少了启动时的内存占用，同时保证了与 Linux `brk` 语义的兼容。

`mmap` 系统调用是更通用的内存映射入口，支持匿名映射、文件映射和共享映射等多种类型。匿名映射（`MAP_ANONYMOUS`）使用 `CowBackend` 后端，按需分配物理页并支持写时复制；私有文件映射同样使用 `CowBackend`，但物理页从文件页缓存获取，首次写入时触发 CoW 分配新页；共享文件映射使用 `FileBackend`，直接与页缓存关联，写入同步到文件；共享匿名映射使用 `SharedBackend`，通过 `SharedPages` 结构支持跨进程共享。`mmap` 还支持 `MAP_FIXED`、`MAP_POPULATE` 等标志控制地址选择和预分配行为。

写时复制（Copy-on-Write, CoW）是内存管理的关键优化机制，主要用于 `fork` 后的地址空间克隆。当父进程 fork 子进程时，`AddrSpace::try_clone` 方法遍历所有内存区域，为新地址空间创建共享同一物理页的映射，并将双方映射的权限改为只读。当任一方首次写入某页时，缺页处理逻辑分配新物理页、复制原页内容、更新映射指向新页并恢复写权限。这一机制使 fork 代价从复制全部内存降低到仅建立映射关系，大幅提升了进程创建性能。

缺页处理由 `AddrSpace::handle_page_fault` 实现，根据发生缺页的地址找到对应内存区域，调用后端的 `populate` 方法分配物理页并建立映射。对于 `CowBackend`，populate 检查是否需要复制原页；对于 `FileBackend`，populate 从页缓存读取文件内容。缺页处理完成后，用户程序可继续执行，整个过程对用户态完全透明。这一懒分配-缺页处理的组合构成了现代操作系统内存管理的基本范式，StarryOS 的实现沿用了这一设计并在此基础上进一步优化。

== 文件系统

StarryOS 的文件系统设计遵循类 Unix 系统常见的 VFS 分层思路，即通过统一抽象将具体物理文件系统、伪文件系统以及设备文件纳入同一访问框架。在这一框架下，文件描述符负责向进程暴露统一的访问句柄，`file` 对象承载一次打开后的状态，`inode` 与目录项等对象则承担对底层命名空间和存储实体的组织。这种设计的意义在于，将进程看到的文件操作接口与具体文件系统实现解耦，从而能够在相同系统调用语义下支持普通磁盘文件、管道、套接字以及 `/proc`、`/dev` 等特殊文件。

StarryOS 既需要处理普通文件系统，也需要实现 `procfs`、`tmpfs`、`devfs` 等伪文件系统支持。因此，VFS 层不仅是功能复用的基础，也是后续缓存优化的重要承载点。页缓存、块缓存和目录项缓存等机制虽然将在后文重点讨论，但它们能够发挥作用的前提，正是系统已经建立起统一的文件对象与路径解析框架，使不同来源的数据访问都可以被纳入一致的管理路径中。

== 网络与终端子系统

网络与终端子系统共同构成了 StarryOS 面向真实用户空间应用的重要外部交互能力。系统网络栈建立在 `smoltcp` 之上，并在其外封装 POSIX 风格的 socket 抽象，以支持 TCP、UDP 和 Unix Domain Socket 等常见通信方式；同时，系统还逐步补齐了多设备接入和与事件机制协同工作的能力。网络相关逻辑分布在文件抽象和底层网络模块的结合处，反映出"套接字即文件对象"的 Unix 风格设计思路。

终端相关能力则主要面向交互式应用和开发工具链支持。为了运行 shell、编辑器以及更复杂的图形与文本应用，系统需要提供 tty/pty、行规程以及作业控制等基础机制，使应用能够在接近 Linux 的终端语义下运行。这些能力本身未必体现出特别强的创新性，但它们对于兼容性内核的完整性至关重要，也是后续展示 Alpine Linux、开发工具链和图形环境支持时不可缺少的系统基础。

== 信号与同步机制

信号与同步机制贯穿于 StarryOS 的多个子系统之中，是实现类 Linux 进程交互语义的重要支撑。信号机制由独立的 `starry_signal` crate 实现，负责在异步事件发生时向目标进程或线程传递通知，并要求系统在合适的边界上检查待处理信号、构造用户态处理上下文并恢复执行；同步机制则需要为进程阻塞、等待和唤醒提供统一基础，包括 futex、poll/select/epoll 等常见设施。

信号机制采用进程级和线程级两层管理结构。`ProcessSignalManager` 维护进程级待处理信号队列（`PendingSignals`）、信号处理器表（`SignalActions`）以及所有线程信号管理器的弱引用列表。信号处理器表是一个 64 元素的数组，每个元素记录对应信号的处理器地址、掩码和标志（如 `SA_RESTART`、`SA_SIGINFO` 等）。`ThreadSignalManager` 维护线程级待处理队列、信号掩码（`blocked`）和信号栈（`stack`），并在构造时自动注册到进程管理器的弱引用列表中。当线程退出时，其弱引用自动失效，无需显式注销。

待处理信号结构（`PendingSignals`）区分标准信号和实时信号的处理方式。标准信号（1--31）每种最多一个实例待处理，使用固定大小的 `Option<Box<SignalInfo>>` 数组存储，后续发送的相同信号被丢弃。实时信号（32--64）可排队多个实例，使用 `VecDeque<SignalInfo>` 数组存储，保证不丢失。信号集（`SignalSet`）使用位图记录当前待处理的信号编号，支持高效的集合操作。信号出队时根据掩码选择编号最小的待处理信号，并自动更新信号集。

信号投递分为进程级和线程级两种路径。当向进程发送信号时（如 `kill` 系统调用），`ProcessSignalManager::send_signal` 方法首先检查该信号是否被忽略，若未被忽略则将信号加入进程队列，随后遍历线程弱引用列表找到第一个未阻塞该信号的线程进行唤醒。若所有线程都阻塞该信号，则信号保留在进程队列中等待解禁。当向线程发送信号时（如 `tgkill` 系统调用），`ThreadSignalManager::send_signal` 方法直接将信号加入线程队列并唤醒线程。

为优化信号检查性能，进程级和线程级管理器分别维护 `possibly_has_signal` 原子布尔变量。当信号入队时设置为 `true`，当信号出队且队列为空时设置为 `false`。在系统调用返回路径检查信号时，`ThreadSignalManager::check_signals` 方法首先读取该变量进行快速判断：若两个变量都为 `false`，则直接返回不进行任何处理；仅当至少一个为 `true` 时才进入完整的出队和处理流程。这一设计避免了每次系统调用返回时都进行锁操作和队列检查的开销。

信号处理时需构造用户态信号帧（`SignalFrame`），包含保存的用户上下文（`UContext`）、信号信息（`SignalInfo`）和原用户上下文（`UserContext`）。帧被写入用户信号栈，随后用户上下文的指令指针被设置为用户定义的处理器地址，栈指针指向信号帧，参数寄存器设置为信号编号、信号信息指针和上下文指针。处理器返回地址被设置为信号蹦床（signal trampoline）代码，该代码在系统初始化时复制到用户地址空间的固定位置，负责调用 `rt_sigreturn` 系统调用恢复原有执行流。

futex 机制为用户态同步原语提供内核级等待和唤醒能力。每个进程维护一个 futex 表，以用户地址为键存储等待队列（`WaitQueue`）。`FUTEX_WAIT` 操作检查用户地址的值，若匹配则阻塞当前线程；`FUTEX_WAKE` 操作唤醒指定数量的等待者。futex 与信号机制的交互体现在等待过程可被信号打断，此时系统调用返回错误码 `EINTR`，用户态需要据此决定是否重试或执行其他逻辑。若信号处理器设置了 `SA_RESTART` 标志，内核会自动重启被中断的系统调用。

从系统组织角度看，信号机制并不是一个孤立模块，而是与系统调用返回路径、任务状态切换以及 I/O 事件处理紧密结合。特别是在阻塞系统调用场景下，信号能否正确打断等待过程、系统是否能够按照预期返回错误码或执行重启语义，直接决定了用户态应用的兼容性表现。`starry_signal` 作为独立 crate 设计，将信号管理逻辑与具体内核实现解耦，便于测试和复用。

== 本章小结

本章介绍了 StarryOS 作为 Linux 兼容性内核所必须具备的核心子系统，包括系统调用框架、任务与进程管理、内存管理、文件系统、网络与终端以及信号与同步机制。这些子系统构成了系统的"基本盘"，使 StarryOS 能够承载用户态程序加载、多进程并发执行、文件和网络 I/O 以及进程间通信等典型应用场景。

从实现角度看，这些子系统的共同特点是在复用 ArceOS 底层能力的基础上补充 Linux 兼容语义。任务调度、等待队列、文件系统框架和网络栈由 ArceOS 或其依赖模块提供，StarryOS 通过 `extern_trait` 机制扩展任务状态、通过 `ProcessData` 组织进程级资源、通过统一的 `FileLike` 抽象整合各类 I/O 对象。这种分层协作的设计使系统在保持组件化架构的同时，逐步对齐 POSIX/Linux 的复杂语义要求。

值得注意的是，部分子系统被抽取为独立 crate 以提高复用性。`starry_process` 实现进程关系管理，包括进程组、会话和线程组；`starry_signal` 实现信号机制，区分进程级和线程级管理；`weak-map` 提供弱引用映射，支持会话-进程组-进程层级结构的隐式生命周期管理。这些 crate 可以被其他需要类似功能的系统直接复用，体现了组件化设计在工程实践中的价值。

这些基础子系统虽然本身未必体现强烈的创新性，但它们为后续关键机制与性能优化工作提供了必要的承载平台。用户态访存优化、异步 I/O 机制、多级缓存设计以及信号与事件等待的配合，都需要建立在完整且语义正确的子系统之上。下一章将聚焦这些影响系统结构和性能上限的关键机制，说明 StarryOS 如何在组件化实现与性能优化之间取得平衡。

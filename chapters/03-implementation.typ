#import "../template.typ": todo

= 系统实现

#todo[
  开头一段：说明本章在第2章设计的基础上，介绍 StarryOS 兼容性内核的具体实现，
  以及多级缓存、多核调度、虚拟内存等性能优化的实现情况。
]

== 系统调用层

#todo[
  介绍系统调用的整体处理流程（用户态陷入 → 分发 → 处理 → 返回），
  以及如何在组件化架构下组织系统调用模块。可附处理流程图。
]

=== 进程与任务管理

#todo[
  简述 fork/exec/wait/exit/clone 等核心进程管理系统调用的实现要点，
  重点说明与 ArceOS 任务模型的对接方式，以及 TaskExt（extern_trait）的应用。
]

=== 内存管理

#todo[简述 mmap/munmap/brk 等的实现，涵盖匿名映射、文件映射、懒分配、CoW 等机制。]

=== 其他系统调用

#todo[
  一段话概括文件 I/O、网络、同步（futex）等模块的实现思路，
  说明遵循 Linux 语义实现了 200+ 个系统调用，不逐一展开。
]

== 文件系统

#todo[
  介绍 VFS 层设计（支持物理 FS 与伪 FS 统一抽象，参考 Linux 设计）。
  重点说明页缓存、块缓存、目录项缓存三层协同加速的设计。
]

== 网络、终端与其他子系统

#todo[
  一段话带过：网络栈基于 smoltcp，支持 TCP/UDP/UDS 及多设备路由；
  终端支持 tty/pty、line discipline、job control；实现思路与主流兼容内核无本质差别。
]

== 信号机制

#todo[
  说明信号投递时机（系统调用返回前检查）、与异步机制的交互
  （信号可中断阻塞的系统调用），以及对 SA_RESTART 等语义的处理。
]

== 多级缓存优化

#todo[
  展开说明页缓存（page cache）、块缓存（block cache）、目录项缓存
  （dcache）三层缓存的实现细节，以及脏页写回策略。
]

== 多核调度优化

#todo[
  说明当前调度实现的基础（基于 ArceOS axtask），
  以及针对多核场景规划/已实施的优化（负载均衡、调度延迟等）。
  如工作尚未完成，说明设计方向和参考（KRR, OSDI'25）。
]

== 虚拟内存优化

#todo[
  说明当前虚拟内存实现（VMA + 页表双层结构），
  以及参考 CortenMM（SOSP'25）取消独立 VMA 抽象的优化方向。
  如工作尚未完成，说明设计思路。
]

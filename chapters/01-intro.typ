#import "../template.typ": todo

= 绪论

== 研究背景

=== 操作系统内核架构的演进

宏内核、微内核和 Unikernel 是操作系统研究中最具代表性的三类内核架构。宏内核将进程管理、内存管理、文件系统、网络协议栈和设备驱动等大多数核心功能置于同一特权地址空间内实现，其优势在于模块间调用路径短、运行时开销低，因而长期在通用计算场景中占据主导地位；但其缺点也较为明显，即系统规模扩大后内部耦合增强，故障隔离与安全边界相对薄弱。微内核则将尽可能少的机制保留在内核态，仅在内核中维护地址空间、线程调度和进程间通信等最基本功能，而将文件系统、驱动等服务下沉到用户态。该设计有利于提高可靠性、可维护性和隔离性，但频繁的跨边界通信会带来额外开销，实现复杂度也更高。

Unikernel 则进一步面向特定应用进行裁剪，将应用与所需的最小操作系统服务整体编译为单一镜像运行。与通用操作系统相比，Unikernel 能够显著减少系统体积、缩短启动时间，并降低运行路径中的冗余功能开销，因而在云原生、边缘设备和专用化部署场景中具有吸引力。然而，这种架构通常以牺牲通用性和兼容性为代价，应用迁移与调试支持也相对不足。总体来看，三类架构分别在性能、隔离性、灵活性和兼容性之间做出不同权衡，并不存在适用于全部场景的统一最优方案。随着 AIoT、云边协同等场景对系统形态提出多样化需求，如何在不同架构之间复用基础能力、降低定制成本，成为操作系统设计中的一个重要问题。

=== 组件化操作系统与 ArceOS

组件化操作系统的核心思想，是将内核中的基础服务按照清晰的接口边界拆分为可组合、可替换、可复用的独立组件，从而降低系统耦合度并提升定制效率。与传统以单一内核形态为中心进行扩展的方式相比，组件化设计更强调将调度、内存、文件系统、设备抽象等共性能力沉淀为稳定模块，再根据具体场景组织成不同系统形态。对于需要在性能、隔离性、兼容性和部署成本之间反复权衡的现代应用场景而言，这种设计方法具有较高的工程价值。

ArceOS 正是在这一背景下提出的实验内核。其基本出发点并非围绕某一种固定架构优化，而是将不同内核形态共享的底层服务视为“内核意义上的库”，通过统一的组件边界和初始化后执行逻辑的差异，构造出 Unikernel、宏内核乃至 Hypervisor 等不同系统形态。这一设计理念可概括为 “One Architecture to build all”，即通过一套统一的组件化基础设施支撑多种内核形态的快速构建。该思路为后续在 ArceOS 基础上扩展兼容性内核提供了方法论基础，也为在不同场景间复用已有内核能力提供了新的实现路径。

=== Starry OS 的发展

#todo[梳理 Starry-Next → Starry Mix → StarryOS 主线的演进历程]

== 相关工作

#todo[
  每类2-3句话即可：
  - 其他 Rust OS：Asterinas（Framekernel）、Theseus、Tock
  - Linux 兼容性内核：gVisor、Graphene/Occlum（TEE场景）
  - 性能优化相关：io_uring、CortenMM（SOSP'25最佳论文）
]

== 本文工作

#todo[
  分3-4段简述本文主要工作与贡献，建议分成两层表述：
  + 完整兼容性内核实现：在 ArceOS 基础上实现 StarryOS，支持进程、内存、
    文件系统、网络、终端、信号等核心能力，并实现 200+ Linux 系统调用
  + 关键机制与性能优化：围绕组件化兼容内核中的关键问题，设计并实现
    extern_trait、用户态切换与反转控制流、高效内核-用户交互、异步 I/O、
    多级缓存等机制
  + AI 应用支持探索：面向 AIoT 场景，探索 NPU 驱动与 AI 应用运行支持方向
]

== 论文结构安排

全文共分为七章。第 1 章介绍研究背景、相关工作以及本文的主要研究内容。第 2 章从 AIoT 场景需求与 ArceOS 基础出发，介绍 StarryOS 的系统定位、设计目标与总体架构。第 3 章进一步展开兼容性内核核心子系统的设计与实现，包括系统调用、任务与进程管理、内存管理、文件系统、网络与终端以及信号与同步机制。第 4 章围绕组件化兼容内核实现中的关键技术问题，介绍 extern_trait、用户态切换与反转控制流、高效内核-用户交互、异步 I/O 以及多级缓存等机制与优化。第 5 章讨论面向 AIoT 场景的 AI 应用支持探索。第 6 章从功能完整性、应用兼容性和性能等维度对系统进行实验评测。第 7 章对全文工作进行总结，并展望后续研究方向。
